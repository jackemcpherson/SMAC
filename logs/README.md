# Logs Directory

This directory contains application logs.

## Structure

- Application logs will be written to this directory
- Log files are excluded from version control via .gitignore
- This README ensures the directory structure is preserved in the repository